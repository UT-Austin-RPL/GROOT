# @package _global_

# default:

# train:
#   use_augmentation: true
#   grad_clip: 100
#   loss_scale: 1.0

# eval:
#   eval_every: 5
#   n_epochs: 50
policy:
  pcd_aug:
    network_kwargs:
      mean: 0.0
      std: 0.001
      enabled: True
      use_rotation: True

data:
  obs:
    modality:
      pcd: ["xyz"]