# @package _global_


policy:
  pcd_aug:
    network_kwargs:
      mean: 0.0
      std: 0.001
      use_position: False
      enabled: True
      use_rotation: True

data:
  obs:
    modality:
      pcd: ["xyz"]